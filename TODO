Move variables to separate file
Parameterize a few more things
Terraform the instance deployment

Jenkins:
finish odds and ends
setup backup dokuwiki every hour
setup restore dokuwiki on git update

Terraform:
VPC Stuff
Firewall/Security Group
Create Jenkins from AMI
Create Dokuwiki from AMI

Docoument:
Packer Dokuwiki (from scratch and to restore)
Packer Jenkins (from scratch, to restore itself, and to backup and restore Dokuwiki)
Terraform from scratch AWS instance

Backup Dokuwiki:
Gitlab repository
setup ssh keys on machine and also then as part of packer
if git diff then git add, git commit, git push

Dokuwiki template and provisioning:
/home/centos/.ssh/config and /root/.ssh/config with git host maybe via file provisioner?
move config after copying via file provisioner
set permissions on config to 0400
move gitkey to /home/centos/gitkey and set permissions to 0400
mkdir /scripts
chmod scripts
if clean install then initialize git and set origin (git variable)
if not init install then git clone the repository

secure data directory (and other directories from dokuwiki docs)

on to terraform
